import { SQL, DBC, std } from "wow/wotlk";

//Handles the character starting outfit for each class - copied from Blood Elf
    std.DBC.CharStartOutfit.getRow(10).clone(22,
        {
            //
        })  